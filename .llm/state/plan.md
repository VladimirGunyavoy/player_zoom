# Plan & Progress - План и прогресс

**Last updated:** 2026-04-24

---

## 🎯 Текущая задача

**BoundaryRayFamily — разобраться с визуальной "кашей"**
- Лучи работают, но картинка перегружена — нужно либо скрывать family_b пока, либо делать переключение видимости

---

## ✅ Сделано в сессии 11

- `run.py` + `watcher.py` — фикс путей (PROJECT_ROOT, импорт модуля)
- `config/colors.json` — создан, ColorManager теперь читает его (фикс пути)
- `src/core/line_manager.py` — новый класс для ScalableLine объектов
- `src/spores/ghost_spore_family.py` — рёбра графа (time + control + root→gen1)
  - `_GhostLineFamily` внутри файла
  - Параметры `name`, `time_sign`, `color_key` для поддержки нескольких семей
  - Свойства `nodes`, `n_tau`, `n_u`, `time_sign`
  - Граничные ноды (j=±n_u) подсвечены отдельным цветом
- `main.py` — `family_b` (обратное время), `SharedContext` с менеджерами как полями
- `src/spores/boundary_ray_family.py` — новый файл
  - `BoundaryRay` — один луч из граничной точки с противоположным управлением
  - `BoundaryRayFamily` — семья лучей из одной границы (plus_u / minus_u)

---

## 📅 Краткосрочные цели

### 1. Разобраться с визуальной "кашей"
**Статус:** Не начата

- Слишком много объектов одновременно (family_a + family_b + лучи)
- Варианты: переключение видимости клавишами, или пока отключить family_b
- Следующий агент должен разобраться с этим вместе с пользователем

### 2. Git коммит сессии 11
**Статус:** Не сделан

---

## 🗓️ Среднесрочные цели

### BoundaryRayFamily второго поколения
- Сейчас лучи строятся из family_a
- Аналогично добавить для family_b
- Возможно нужна архитектурная доработка BoundaryRayFamily

### Добавить numba
- [ ] `pip install numba`, `@njit` к `_step` в `double_integrator.py`
- **Приоритет:** Низкий

### Создать config/json/colors.json
- [x] Уже сделано — `config/colors.json` создан и подключён

---

## 🔮 Долгосрочные цели

- Нелинейная динамика вместо DoubleIntegrator
- Интеграция с основным проектом v16_picker
- Юнит-тесты для математики

---

## 🎨 Backlog

### Visualization
- Визуализация invariant point на полу
- Grid с масштабируемыми числами
- Стрелки вместо линий для рёбер

### Camera & Navigation
- Сохранение/загрузка позиции камеры
